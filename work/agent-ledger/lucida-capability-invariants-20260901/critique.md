# Self-critique

- finding: restored state accepted duplicate or missing capability reports
- decision: enforce the same three-name invariant already required by the orchestrator
- schema: cap the serialized list at exactly three entries; runtime checks names and uniqueness
- validation needed: duplicate, missing, generated XIO state, full suite, and schema graph
